import 'package:flutter/material.dart';

class GorevlerIcerik extends StatelessWidget {
  const GorevlerIcerik({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Görevler İçeriği'),
    );
  }
}
