import 'package:flutter/material.dart';

class AyarlarIcerik extends StatelessWidget {
  const AyarlarIcerik({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ayarlar İçeriği'),
    );
  }
}
